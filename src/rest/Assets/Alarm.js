import { getAlarm, readRecord, postRecordDisappear } from '../api/Alarm';
import { loadProjectSiteTree } from '../api/Tree';
import { generateTicket } from '../api/Ticket';
export default {
  getAlarm,
  loadProjectSiteTree,
  readRecord,
  generateTicket,
  postRecordDisappear
};
